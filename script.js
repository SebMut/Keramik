const buttons=[...document.querySelectorAll('.switcher button')];const themes=['one','two','three','four','five'];function setTheme(t){document.body.classList.remove(...themes);if(t!=='one')document.body.classList.add(t);buttons.forEach(b=>b.classList.toggle('active',b.dataset.theme===t));localStorage.setItem('studio8-theme',t)}buttons.forEach(b=>b.addEventListener('click',()=>setTheme(b.dataset.theme)));setTheme(localStorage.getItem('studio8-theme')||'one');document.querySelectorAll('a[href^="#"]').forEach(a=>a.addEventListener('click',e=>{const el=document.querySelector(a.getAttribute('href'));if(el){e.preventDefault();el.scrollIntoView({behavior:'smooth'})}}));